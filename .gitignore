node_modules/
dist/
.env
.env.local
.DS_Store
*.log
.vite/
.vercel

# Raw workout exports (personal GPS/heart-rate data) — keep local, never commit.
# Only the aggregated src/data/stats.json is committed.
data/*.csv
